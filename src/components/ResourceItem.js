import React from 'react';

const ResourceItem = () => {
  return (

    <div className="tilegrid" style={{display: 'flex'}} >
      <div className="tile"  style={{backgroundColor: '#ddd', border: '1px solid #ccc'}} >
        <h1 className="title">React (i)</h1>
      </div>
    </div>

  );
};

export default ResourceItem;
